"""Tests for Phase 3 (integrity of the exported graph)."""
# TODO: no orphaned nodes, no negative/NaN weights, every transfer
# station_id has inter-layer degree > 0, edge count per layer consistent
# with trip counts from routes.txt.
